{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "playwright",
  "description": "Capability for playwright",
  "windows": ["main"],
  "permissions": [
    "playwright:default"
  ]
}
